{
  "name": "langar-bar-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:native": "node scripts/prepare-native-web.mjs",
    "cap:add:android": "npx cap add android",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npm run prepare:native && npx cap sync",
    "cap:sync:android": "npm run prepare:native && npx cap sync android",
    "cap:sync:ios": "npm run prepare:native && npx cap sync ios",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "onesignal-cordova-plugin": "^5.2.9"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  },
  "description": "Langar Bar customer ordering, Langar Club rewards, reservations and secure staff/admin operations app"
}
